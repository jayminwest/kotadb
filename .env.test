# Test Environment Configuration
# Auto-generated for local test database

# Test database connection (PostgreSQL via docker-compose)
SUPABASE_URL=http://localhost:5433
SUPABASE_SERVICE_KEY=test-service-key-local
SUPABASE_ANON_KEY=test-anon-key-local

# Database connection details
DB_HOST=localhost
DB_PORT=5433
DB_NAME=kotadb_test
DB_USER=postgres
DB_PASSWORD=postgres

# Direct PostgreSQL connection string for migrations
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/kotadb_test
