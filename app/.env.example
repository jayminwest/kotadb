# ============================================================================
# KotaDB v2.0.0 - Local-Only Configuration
# ============================================================================
#
# This version of KotaDB operates exclusively in local mode using SQLite.
# No cloud services, authentication, or external dependencies required.
#
# Copy this file to .env to configure your local installation.
# ============================================================================

# ============================================================================
# Database Configuration
# ============================================================================

# Local SQLite database path (optional override)
# Default: .kotadb/kota.db (project-local, auto-detected via .git marker)
# 
# Use this to:
#   - Override default project-local behavior
#   - Use a shared database across multiple projects
#   - Specify absolute path for database location
# 
# Examples:
#   KOTADB_PATH=.kotadb/kota.db              # Explicit project-local (same as default)
#   KOTADB_PATH=/shared/databases/kota.db    # Shared across projects
#   KOTADB_PATH=~/.kotadb/kota.db            # Global (legacy behavior)
# 
# KOTADB_PATH=/custom/path/to/kota.db

# Local JSONL export directory (optional override)
# Default: .kotadb/export/ (project-local)
# 
# Use this to:
#   - Export to a different location
#   - Share exports across projects
# 
# KOTADB_EXPORT_PATH=/custom/path/to/export

# ============================================================================
# Server Configuration
# ============================================================================

# Port for the MCP server (optional)
# Default: 3000
PORT=3000

# ============================================================================
# Indexer Configuration
# ============================================================================

# Number of files to process in a single database transaction during indexing
# Larger batches: Fewer database calls but higher memory usage
# Smaller batches: More overhead but better progress granularity
# Default: 50
# INDEXER_BATCH_SIZE=50
